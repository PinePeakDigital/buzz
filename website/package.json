{
  "name": "website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "sync:demo": "node -e \"require('fs').copyFileSync('../scripts/demo/demo.gif','public/demo.gif')\"",
    "dev": "pnpm run sync:demo && astro dev",
    "start": "pnpm run sync:demo && astro dev",
    "build": "pnpm run sync:demo && astro build",
    "preview": "pnpm run sync:demo && astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.39.3",
    "astro": "^6.3.1",
    "sharp": "^0.34.5"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp"]
  }
}