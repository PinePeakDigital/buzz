name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      version:
        description: 'Version override (e.g., v1.2.3). Leave empty to auto-calculate from conventional commits.'
        required: false
        type: string

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch tags
        run: git fetch --prune --tags

      - name: Calculate semantic version
        id: semver
        if: github.event.inputs.version == ''
        uses: ietf-tools/semver-action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          fallbackTag: '0.0.0'
          noVersionBumpBehavior: silent
          skipInvalidTags: true

      - name: Determine version
        id: version
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            echo "Using manual version override: ${{ github.event.inputs.version }}"
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
            echo "skip=false" >> $GITHUB_OUTPUT
          elif [ -n "${{ steps.semver.outputs.next }}" ]; then
            echo "Using auto-calculated version: ${{ steps.semver.outputs.next }}"
            echo "version=${{ steps.semver.outputs.next }}" >> $GITHUB_OUTPUT
            echo "skip=false" >> $GITHUB_OUTPUT
          else
            echo "No conventional commits found and no manual version provided - skipping release"
            echo "version=" >> $GITHUB_OUTPUT
            echo "skip=true" >> $GITHUB_OUTPUT
          fi

      - name: Check if release is needed
        id: check
        run: |
          echo "skip=${{ steps.version.outputs.skip }}" >> $GITHUB_OUTPUT
          if [ "${{ steps.version.outputs.skip }}" = "false" ]; then
            echo "Version to release: ${{ steps.version.outputs.version }}"
          fi

      - name: Set up Go
        if: steps.check.outputs.skip == 'false'
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Build cross-platform binaries
        if: steps.check.outputs.skip == 'false'
        run: ./scripts/build.sh
        env:
          VERSION: ${{ steps.version.outputs.version }}

      - name: Create Release
        if: steps.check.outputs.skip == 'false'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: Release ${{ steps.version.outputs.version }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            buzz-linux-amd64
            buzz-linux-arm64
            buzz-darwin-amd64
            buzz-darwin-arm64
            buzz-windows-amd64.exe
            buzz-windows-arm64.exe
            README.md
            LICENSE
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
